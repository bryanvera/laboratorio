git init
git remote add origin <link>
git config --global user.name "Nombre de la persona"
git cofnig --global user.email "Email de la persona"
git add "Nombre del archivo"
git commit -m "Documentaci√on del archivo"
git push origin master
Usar git --version en cmd, en caso de no estar, abrir "git bash" y repetir el comando.
git clone <link> para clonar un repositorio
